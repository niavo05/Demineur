import { StyleSheet } from "react-native";

export const boxStyles = StyleSheet.create({
  squareBox: {
    width: 20,
    height: 20,
    backgroundColor: "red",
    borderColor: "black",
    borderWidth: 1,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  bombImage: {
    width: 15,
    height: 15,
  },
});
